# AIGuidebook Test Plan

## User Request
Generate a complete test plan for the AIGuidebook system, starting with Section 1: Test Case Table.

---

## Section 1: Test Case Table

| Test ID | Test Type | Requirement | Input / Trigger | Expected Output | Test Method | Pass/Fail Criteria | Notes |
|---------|-----------|-------------|-----------------|-----------------|-------------|-------------------|-------|
| UT-01 | Unit | FR-26 | Call `buildEntriesFromLog()` with a populated interaction log (3 entries with tool_name, category, description) | Returns 3 DeclarationEntry objects, each with origin `auto-generated` and content formatted as "tool_name was used for category: description" | Jest/Vitest unit test | Each returned entry has `origin === 'auto-generated'` and content matches the expected template for all 3 logs | Tests the core transformation logic in DraftEditorModule |
| UT-02 | Unit | FR-26 | Call `buildEntriesFromLog()` with an empty interaction log array (`[]`) | Returns an empty array; no entries are generated | Jest/Vitest unit test | Return value is `[]` with length 0; no errors thrown | Negative/edge case — ensures graceful handling when student has no AI interactions logged |
| UT-03 | Unit | FR-27 | Call `computeStats()` with 5 scoped interaction logs across 2 tools ("ChatGPT", "Copilot") and 3 categories | Returns object with `toolCount: 2`, `interactionCount: 5`, `categories` map with 3 keys, and correct `timeSpan` from earliest to latest `logged_at` | Jest/Vitest unit test | `toolCount === 2`, `interactionCount === 5`, `categories` has exactly 3 entries, `timeSpan.start` equals earliest log date, `timeSpan.end` equals latest log date | Tests statistics derivation logic |
| UT-04 | Unit | FR-27 | Call coverage calculation with 3 declared entries and 5 total logged interactions | Returns coverage value of 60% | Jest/Vitest unit test | `coverage === 0.6` (or 60%); coverage threshold highlight triggered at exactly 60% boundary | Boundary test for the 60% discrepancy threshold |
| UT-05 | Unit | FR-30 | Call `countWords("This is a valid reflection with enough words to pass the minimum threshold of twenty-five words for the prompt")` | Returns word count ≥ 25 | Jest/Vitest unit test | `countWords()` returns exactly the number of whitespace-separated non-empty tokens; result ≥ 25 | Tests word counting utility in ReflectionModule/validation.ts |
| UT-06 | Unit | FR-30 | Call `validatePrompt("")` with an empty string | Returns `{ valid: false }` with error indicating insufficient word count | Jest/Vitest unit test | `valid === false` and error message references word count requirement | Negative case — empty reflection should fail validation |
| UT-07 | Unit | FR-30 | Call `hasRepetition("I used AI I used AI I used AI")` with a phrase containing 3-word n-gram repeated 3 times | Returns `true` indicating repetition detected | Jest/Vitest unit test | `hasRepetition()` returns `true` | Tests repetition detection to prevent low-effort reflections |
| UT-08 | Unit | FR-30 | Call `validateReflection()` with prompt1 valid (≥25 words, no repetition) and prompt2 empty | Returns `{ valid: false }` because prompt2 is incomplete | Jest/Vitest unit test | `valid === false`; error references prompt2 | Both prompts must be valid for overall reflection to pass |
| UT-09 | Unit | FR-29 | Simulate editing an entry content field to an empty string and triggering save validation | Validation rejects the edit; content field is required | Jest/Vitest unit test | Save is rejected or error state set; entry content is not updated to empty string | Negative case — blank edits should be prevented |
| IT-01 | Integration | FR-26 | Student clicks "Generate Draft" button with 4 scoped interaction logs available from the API | DraftEditorModule calls `POST /api/declarations` then `POST /api/declarations/:id/entries` for each log; UI renders 4 EntryRow components with origin badges showing "auto-generated" | React Testing Library with mocked API | 4 EntryRow components rendered; each displays correct content and `auto-generated` origin badge; `POST` called 4 times for entries | Tests full draft generation flow from button click through API calls to rendered output |
| IT-02 | Integration | FR-26, FR-27 | Student generates a draft from 5 interaction logs; StatsPanel is mounted alongside DraftEditorModule | StatsPanel receives updated entry count via lifted state; displays `interactionCount: 5`, correct tool names, and coverage percentage | React Testing Library with mocked API and event bus | StatsPanel shows "5 interactions", lists all unique tool names, displays coverage as "100%" (5 declared / 5 logged) | Cross-module integration: DraftEditorModule → StatsPanel sync |
| IT-03 | Integration | FR-29, FR-32 | Student clicks edit on an auto-generated entry, changes content from "ChatGPT was used for research: literature review" to "ChatGPT was used briefly for background reading", then saves | `PATCH /api/declarations/:id/entries/:entryId` called with new content; entry origin updates from `auto-generated` to `auto-generated-modified`; `previous_content` preserved; `diff_delta` calculated | React Testing Library with mocked API | API PATCH called once; response shows `origin: 'auto-generated-modified'`; `previous_content` equals original text; `diff_delta` is negative (shorter text); ENTRY_MODIFIED event emitted | Tests inline editing flow including origin badge transition and diff tracking |
| IT-04 | Integration | FR-30, FR-28 | Student fills in both reflection prompts with valid text (≥25 words each, no repetition), then navigates to Review step | ReflectionContext updates `isValid: true`; `PATCH /api/declarations/:id/reflection` called; ReviewAggregator renders reflection text and shows no "reflection incomplete" warning | React Testing Library with mocked API and context providers | Reflection PATCH called with both prompts; ReviewAggregator displays both reflection answers; no "incomplete reflection" alert rendered; confirmation checkbox is enabled | Cross-module: ReflectionModule → ReflectionContext → ReviewAggregator |
| IT-05 | Integration | FR-28 | Student navigates to Review step; declaration has 3 entries, 1 manual entry, valid reflection, and 1 active IntegrityMonitor warning (coverage_low) | ReviewAggregator renders all 3 entries, 1 manual entry, reflection text, and 1 warning banner; confirmation checkbox text changes to acknowledge warnings; submit button disabled until checkbox checked | React Testing Library with mocked API and WarningsContext | All entries rendered; warning banner shows "coverage_low" message; checkbox label includes warning acknowledgement text; submit button has `disabled` attribute until checkbox is checked | Tests full review aggregation with warnings present |
| ST-01 | System | FR-26, FR-29, FR-30, FR-28 | End-to-end scenario: Student (1) generates draft from 3 logged interactions, (2) edits one entry, (3) adds reflections to both prompts, (4) reviews declaration, (5) checks confirmation, (6) submits | Declaration status changes to `submitted`; version history contains snapshots for `initial_open`, `review_step`, and `submission`; all 3 entries persisted (1 modified); reflection saved; no data loss | Cypress E2E test or manual walkthrough against running server | `GET /api/declarations/:id` returns `status: 'submitted'`; `GET /api/version-history/:id` returns ≥3 snapshots; entry with modified content has `origin: 'auto-generated-modified'`; reflection `is_valid: 1` | Happy path end-to-end covering FR-26 → FR-29 → FR-30 → FR-28 |
| ST-02 | System | FR-26, FR-28 | End-to-end scenario: Student generates draft with 0 logged interactions (empty log), attempts to proceed through all steps to submission | Draft generates with 0 entries; StatsPanel shows 0 interactions; student can still add manual entries and reflections; submission succeeds if reflection is valid | Cypress E2E test or manual walkthrough | Declaration created with 0 auto-generated entries; no errors at any step; if reflection is valid, submission returns 200; version history snapshots created | Edge case — empty interaction log should not block the workflow |
| ST-03 | System | FR-30, FR-28 | End-to-end scenario: Student attempts to submit declaration without filling in reflections | Server returns 422 with code `REFLECTION_INVALID`; submission is blocked; student sees error feedback | Cypress E2E test or manual walkthrough | `POST /api/declarations/:id/submit` returns HTTP 422; response body contains `code: 'REFLECTION_INVALID'`; UI displays error message; declaration `status` remains `draft` | Negative system test — server-side validation gate for reflections |
| NFR-01 | Non-functional | NFR-47 | Open the application; click the Help button in the header | HelpSection modal opens displaying guidance content with title and collapsible sections; content describes each major feature (draft generation, editing, reflections, review/submit) | Manual inspection + automated presence check (React Testing Library) | Help modal renders; title matches `guidance.json` helpSection.title; at least 3 collapsible sections present; each section heading and body are non-empty; content accurately describes actual application behavior | Tests that help/explainability content exists and is accurate |
| NFR-02 | Non-functional | NFR-47 | Inspect each major module (DraftEditorModule, ReflectionModule, ManualUsageModule, ReviewAggregator) for tooltips and hint text | Each module displays contextual help text (tooltips on buttons, hint text near form fields) loaded from `guidance.json` | Manual inspection + automated check that `useGuidance()` hook returns non-empty tooltips/hints for each module key | Tooltips object in `guidance.json` has keys for each major feature; `useGuidance()` returns these values; tooltip/hint elements rendered in DOM with matching text | Verifies explainability at the feature level, not just global help |
| NFR-03 | Non-functional | NFR-48 | Complete a full declaration workflow (generate draft → edit → reflect → review → submit) using only keyboard navigation (Tab, Enter, Space, Arrow keys) | All interactive elements are reachable and operable via keyboard; focus order is logical (top-to-bottom, left-to-right within each step); focus is visible on every interactive element | Manual task completion test + automated a11y check (jest-axe) | All buttons, textareas, checkboxes, and links reachable via Tab; Enter/Space activates controls; no keyboard traps; focus indicator visible on every focused element; jest-axe reports 0 violations | Keyboard accessibility is a core usability requirement per WCAG 2.1 AA |
| NFR-04 | Non-functional | NFR-48 | Run automated accessibility audit on each rendered module (DraftEditorModule, StatsPanel, ReflectionModule, ManualUsageModule, ReviewAggregator, HelpSection) | No WCAG 2.1 AA violations detected | jest-axe automated check via `toHaveNoViolations()` | `expect(container).toHaveNoViolations()` passes for each module; all form fields have associated labels; all images/icons have alt text; color contrast meets 4.5:1 ratio; aria-live regions present for dynamic content | Automated accessibility regression testing |
| NFR-05 | Non-functional | NFR-48 | Heuristic evaluation against Nielsen's 10 usability heuristics: evaluate the multi-step wizard for visibility of system status, match between system and real world, user control/freedom, consistency, error prevention | Report documenting findings for each heuristic with severity ratings | Manual heuristic evaluation by 2+ evaluators | Each heuristic evaluated and documented; no severity-3+ (major) issues; step indicator shows current position (visibility); domain language used consistently (match); back navigation available (user control); button styles consistent (consistency); confirmation gate prevents accidental submission (error prevention) | Qualitative usability assessment complementing automated tests |
